public class Ingredient {
    // TODO: Implement Ingredient. Add useful variables, methods, getters, setters
    // (if needed), and constructor(s),  and a toString() method.
    // Hint: An ingredient has a name and an effect on the potion's strength.
    // Enforce max. 10 (where does that check belong if ingredients do not change after creation?)

}
